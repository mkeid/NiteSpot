$.ajaxSetup({
    'beforeSend': function(xhr) {
        xhr.setRequestHeader('X-CSRF-Token', $("meta[name='csrf-token']").attr('content'));
    }
});

// The following renders basic user information.
var user_gender = getCookie("gender");
var user_name_first = getCookie("name_first");
var user_name_last = getCookie("name_last");
var user_privacy = getCookie("privacy");
var user_year = getCookie("year");

// The following renders basic account information.
var account_id = getCookie("id");
var account_type = getCookie("account_type");